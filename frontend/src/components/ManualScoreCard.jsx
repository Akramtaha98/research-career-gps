// Deprecated: replaced by ScoreBox.jsx, which splits this into two
// independent Scopus/WOS slots instead of one source-toggle. Kept as a
// re-export only so nothing breaks if something still imports the old name;
// prefer importing ScoreBox directly in new code.
export { default } from './ScoreBox';
